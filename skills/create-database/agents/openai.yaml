interface:
  display_name: Create Managed Database
  short_description: Provision PostgreSQL, MySQL, MariaDB, ClickHouse or Valkey as a managed service with a credential Secret
  brand_color: '#059669'
policy:
  allow_implicit_invocation: true
