interface:
  display_name: Manage Database Credentials
  short_description: Deliver managed service credentials as Secrets and rotate them on demand or on a schedule
  brand_color: '#1d4ed8'
policy:
  allow_implicit_invocation: true
