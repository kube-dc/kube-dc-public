apiVersion: kube-dc.com/v1
kind: FIp
metadata:
  name: {fip-name}
  namespace: {project-namespace}
spec:
  externalNetworkType: public
  vmTarget:
    vmName: {vm-name}
    interfaceName: default    # Must match the VM's network interface name
