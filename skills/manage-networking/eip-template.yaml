apiVersion: kube-dc.com/v1
kind: EIp
metadata:
  name: "{eip-name}"
  namespace: "{backing-namespace}"
spec:
  # public: internet-routable when the provider exposes that pool
  # cloud: reachable only from configured platform networks
  externalNetworkType: public
