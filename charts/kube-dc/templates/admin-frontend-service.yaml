{{- if .Values.adminFrontend.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-dc.adminFrontend.fullname" . }}
  labels:
    {{- include "kube-dc.adminFrontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.adminFrontend.service.type }}
  ports:
    - port: {{ .Values.adminFrontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-dc.adminFrontend.selectorLabels" . | nindent 4 }}
{{- end }}
